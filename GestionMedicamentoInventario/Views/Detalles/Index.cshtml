@model IEnumerable<GestionMedicamentoInventario.Models.Detalle>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Lote.idLote)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Inventario.idInventario)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Lote.idLote)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Inventario.idInventario)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.idDetalle }) |
                    @Html.ActionLink("Details", "Details", new { id = item.idDetalle }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.idDetalle })
                </td>
            </tr>
        }

    </table>
</body>
</html>
