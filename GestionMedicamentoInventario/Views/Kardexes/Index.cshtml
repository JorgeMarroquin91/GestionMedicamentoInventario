@model IEnumerable<GestionMedicamentoInventario.Models.Kardex>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.saldo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Medicamento.nombreMedicamento)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.saldo)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Medicamento.nombreMedicamento)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.idKardex }) |
        @Html.ActionLink("Details", "Details", new { id = item.idKardex }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.idKardex }) |
        @Html.ActionLink("Mostrar", "mostrarKardex", new { id = item.idKardex })
    </td>
</tr>
}

    </table>
</body>
</html>
