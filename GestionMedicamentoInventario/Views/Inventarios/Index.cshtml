@model IEnumerable<GestionMedicamentoInventario.Models.Inventario>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.existencia)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.existencia)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.idInventario }) |
                    @Html.ActionLink("Details", "Details", new { id = item.idInventario }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.idInventario })
                </td>
            </tr>
        }

    </table>
</body>
</html>
